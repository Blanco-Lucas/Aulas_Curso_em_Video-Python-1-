# from decimal import Decimal
# n1 = float(input('Diga me o valor inteiro: '))
# por = Decimal(1 / (n1 / 5))
# valor = n1 - por
# print(f'Produto com 5% aplicado, com sucesso\n Valor:R${valor:.3}!\n\n *desconto de{por:}*')
import decimal
n1 = float(input('Digame seu numéro: '))
raiz = n1 ** (1 / 2)
print(raiz)
